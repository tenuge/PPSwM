function out = flipvec(x)
%FLIPVEC(X) flips row vectors and column vectors and leaves matrisses
% untouched

[r,c] = size(x);
if r == 1 && c == 1

    = 'scalar';
elseif r == 1 && c > 1
    out = 'row vector';
elseif c == 1 && r > 1
    out = 'column vector';
else
    out = 'matrix';
end 
